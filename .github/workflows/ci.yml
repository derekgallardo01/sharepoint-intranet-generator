name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install pytest
        run: pip install --quiet pytest
      - name: Unit tests
        run: python -m pytest -q
      - name: Eval harness (validation cases)
        run: python evals/run.py
      - name: Smoke-test the demo (Meridian Advisory)
        run: python run.py
      - name: Smoke-test the alternate definition (Acme Manufacturing)
        run: python cli.py generate examples/site-definition-acme-manufacturing.json --out /tmp/acme-intranet
